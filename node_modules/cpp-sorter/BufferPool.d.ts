export declare class BufferPool {
    private pool;
    private bufferSize;
    constructor(bufferSize: number, initialPoolSize?: number);
    private initPool;
    getBuffer(): ArrayBuffer;
    returnBuffer(buffer: ArrayBuffer): void;
}
