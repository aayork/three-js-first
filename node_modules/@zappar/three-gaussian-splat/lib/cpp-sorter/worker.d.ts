/// <reference types="emscripten" />
import type { MainModule } from './ISort';
export declare class WasmSorter {
  private vertexCount;
  private globalBuffer;
  module: EmscriptenModule & MainModule;
  private viewProjPtr;
  private globalBufferPtr;
  private combinedPtr;
  private bufferPool;
  constructor(vertexCount: number, globalBuffer: Uint8Array);
  load(): Promise<void>;
  runSort(viewProj: Float32Array): ArrayBuffer;
  updateGlobalBuffer(globalBuffer: Uint8Array = this.globalBuffer);
  returnBuffer(buffer: ArrayBuffer): void;
  private calculateCombinedLength;
  dispose(): void;
}
