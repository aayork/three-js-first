import * as THREE from 'three';
export declare class GaussianSplatGeometry extends THREE.InstancedBufferGeometry {
    private maxSplats;
    private worker;
    constructor(maxSplats: number);
    private viewProj;
    private _sortRunning;
    loading: boolean;
    private _initialized;
    update(camera: THREE.PerspectiveCamera | THREE.Camera, meshMatrixWorld: THREE.Matrix4): Promise<void>;
    load(url: string, loadingManager?: THREE.LoadingManager): Promise<void>;
    private vertexCount;
    private initAttributes;
    private extractViews;
    dispose(): void;
}
