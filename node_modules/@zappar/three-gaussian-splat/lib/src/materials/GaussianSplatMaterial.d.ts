import * as THREE from 'three';
export declare class GaussianSplatMaterial extends THREE.RawShaderMaterial {
    private currentCamera?;
    private renderer?;
    set sphereRadius(value: number);
    set sphereCenter(value: THREE.Vector3);
    set minAlpha(value: number);
    set planeNormal(value: THREE.Vector3);
    set planeDistance(value: number);
    constructor();
    private onResize;
    dispose(): void;
    initialize(camera: THREE.PerspectiveCamera | THREE.Camera, renderer: THREE.WebGLRenderer): void;
}
