import * as THREE from 'three';
export declare class SplatLoader extends THREE.Loader {
    private processDataCallback?;
    constructor(processDataCallback?: ((data: Uint8Array, bytesRead: number, isComplete?: boolean) => void) | undefined, manager?: THREE.LoadingManager);
    load(url: string, onLoad?: (data: Uint8Array) => void, onProgress?: (event: ProgressEvent) => void, onError?: (event: ErrorEvent) => void): void;
    loadAsync(url: string, onProgress?: (event: ProgressEvent) => void): Promise<{
        data: Uint8Array;
        bytesRead: number;
    }>;
    private _processData;
}
